# Server-only values. Do not prefix these with NEXT_PUBLIC_.
# The browser calls the same-origin /api/rag proxy; this credential is never bundled.
RAG_API_URL=http://127.0.0.1:8000
RAG_API_KEY=replace-with-your-backend-rag-api-key
